import { createMockRouter } from "@/__mocks__/createMockRouter";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { AppRouterContext } from "next/dist/shared/lib/app-router-context.shared-runtime";

describe('Configuracion page - "/cuenta/configuracion"', () => {
  it("Tiene que tener un boton para volver hacia atras.", () => {});

  it("Tiene que tener el titulo de “configuracion”.", () => {});

  it("Tiene que tener la informacion del usuario (mail).", () => {});

  it("Tiene que tener una opcion para cambiar contraseña.", () => {});

  it("Tiene que tener un boton para eliminar la cuenta llamado “eliminar cuenta”.", () => {});
});
